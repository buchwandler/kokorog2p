name: Run tests

on:
  push:
  schedule:
    - cron: "17 3 * * 6"
  workflow_dispatch:

jobs:
  compatibility-smoke:
    name: Smoke (${{ matrix.os }}, Python ${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - {os: ubuntu-latest, python-version: "3.10"}
          - {os: ubuntu-latest, python-version: "3.11"}
          - {os: ubuntu-latest, python-version: "3.12"}
          - {os: ubuntu-latest, python-version: "3.13"}
          - {os: ubuntu-latest, python-version: "3.14"}
          - {os: windows-latest, python-version: "3.13"}
          - {os: macos-latest, python-version: "3.13"}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install espeak-ng (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update -o Acquire::Retries=3
          sudo apt-get install -y espeak-ng
      - name: Install espeak-ng (macOS)
        if: matrix.os == 'macos-latest'
        run: brew install espeak-ng
      - name: Install espeak-ng (Windows)
        if: matrix.os == 'windows-latest'
        shell: bash
        run: choco install espeak-ng -y
      - name: Install smoke-test dependencies
        shell: bash
        run: python -m pip install -e ".[dev]"
      - name: Run compatibility smoke tests
        shell: bash
        run: |
          python -m pytest -q \
            tests/test_attr_parser.py \
            tests/test_base.py \
            tests/test_dependency_contract.py \
            tests/test_markers.py \
            tests/test_pipeline_api.py \
            tests/test_public_api_consistency.py

  full-suite:
    name: Exhaustive isolated suite
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install system dependencies
        run: |
          sudo apt-get update -o Acquire::Retries=3
          sudo apt-get install -y espeak-ng
      - name: Install all test dependencies
        run: |
          python -m pip install -e ".[all,dev]"
          python -m nltk.downloader cmudict
          python -m spacy download en_core_web_sm
      - name: Assert the isolated runner covers every test module
        run: python -m pytest -q tests/test_memory_regressions.py
      - name: Run every test module in a fresh subprocess
        run: python tools/run_test_suite.py --pytest-arg=-q

  lower-bound:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install exact dependency floor
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "spokenform==0.3.4" \
            "g2lex==0.1.8"
          python -m pip install -e ".[dev]"
      - name: Run lower-bound migration tests
        run: |
          python -m pytest -q \
            tests/test_dependency_contract.py \
            tests/test_spokenform_migration.py

  integration:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install system dependencies
        run: |
          sudo apt-get update -o Acquire::Retries=3
          sudo apt-get install -y espeak-ng
      - name: Install packages and routine model
        run: |
          python -m pip install -e ".[all,dev]"
          python -m spacy download en_core_web_sm
          python -m nltk.downloader cmudict
      - name: Run isolated integration suite
        run: python tools/run_test_suite.py --pytest-arg=-q

  model-compatibility:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install English model-test dependencies
        run: python -m pip install -e ".[en,dev]"
      - name: Download supported English models
        run: |
          python -m spacy download en_core_web_sm
          python -m spacy download en_core_web_md
      - name: Run narrow medium-model compatibility checks
        run: |
          python -m pytest -q -m spacy \
            tests/test_en_g2p.py \
            tests/test_normalization.py \
            tests/test_tokenizer.py

  minimal-install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install core package only
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .
          python -m pip install pytest
      - name: Run no-model smoke tests
        run: python -m pytest -q tests/test_minimal_install.py tests/test_span_processing.py

  cross-package:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install optional compatibility packages
        run: |
          python -m pip install -e .
          python -m pip install pytest phrasplit==0.3.4 ssmd==0.8.0
      - name: Run cross-package compatibility tests
        run: python -m pytest -q tests/test_ssmd_phrasplit_integration.py

  japanese-cutlet:
    if: github.event_name != 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install Cutlet backend
        run: python -m pip install -e ".[ja-cutlet,dev]"
      - name: Run Japanese Cutlet smoke tests
        run: python -m pytest -q tests/test_ja_g2p.py -k cutlet

  japanese-cutlet-full-unidic:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install full-UniDic backend
        run: python -m pip install -e ".[ja-cutlet-full,dev]"
      - name: Download UniDic
        run: python -m unidic download
      - name: Run Japanese Cutlet smoke tests
        run: python -m pytest -q tests/test_ja_g2p.py -k cutlet

  lexicon-migration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install package and development dependencies
        run: python -m pip install -e ".[dev]"
      - name: Install exact G2Lex asset builder
        run: |
          python -m pip install "g2lex==0.1.8" "lexhint==0.4.2"
      - name: Download German LexHint dataset
        run: lexhint dataset download de
      - name: Align generated G2Lex lock metadata
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          path = Path("lexicons/lock.json")
          lock = json.loads(path.read_text(encoding="utf-8"))
          lock["g2lex_version"] = "0.1.8"
          for asset in lock["assets"].values():
              asset["generator"] = "g2lex 0.1.8"
          path.write_text(
              json.dumps(lock, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY
      - name: Check deterministic lexicon assets
        run: python scripts/build_g2lex_assets.py --check
      - name: Validate lexicon assets
        run: python scripts/validate_g2lex_assets.py --all --runtime-parity
      - name: Audit German IPA lexicon inventories
        run: |
          python scripts/audit_lexicon_phoneme_inventory.py --id de-de:crane
          python scripts/audit_lexicon_phoneme_inventory.py --id de-de:espeak
          python scripts/audit_lexicon_phoneme_inventory.py --id de-de:olaph
      - name: Build and inspect wheel and sdist
        run: |
          rm -rf dist
          python -m build
          python scripts/check_release_artifacts.py --wheel "$(ls dist/*.whl)" --sdist "$(ls dist/*.tar.gz)"
      - name: Run focused lexicon tests
        run: |
          python -m pytest -q \
            tests/test_lexicon_registry.py \
            tests/test_g2lex_assets.py \
            tests/test_lexicon_selection.py \
            tests/test_g2lex_runtime_lifetime.py \
            tests/test_legacy_lexicon_parity.py \
            tests/test_lexicon_precedence.py \
            tests/test_lexicon_manifest_generation.py \
            tests/test_de_g2p.py \
            tests/test_en_arpabet.py \
            tests/test_cmudict_lexicon.py \
            tests/test_de_g2p_hardening.py \
            tests/test_release_artifacts.py \
            tests/test_dependency_contract.py \
            tests/test_benchmark_de_lexicon_sources.py
